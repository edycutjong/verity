# Use a flat, hoisted node_modules layout instead of pnpm's default symlinked
# store. The `vercel deploy --prebuilt` uploader fails to resolve pnpm's
# .pnpm/* symlinks (ENOENT on e.g. client-only), so a hoisted layout is
# required for the prebuilt deploy to succeed.
node-linker=hoisted
